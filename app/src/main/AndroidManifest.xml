<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <!-- Files/folders come through SAF; TTS is the device's own engine (ADR-003).
         Media-session support (this branch) is the first thing that costs Lector
         real permissions: a foreground service to survive backgrounding while
         reading, and the notification that MediaSession-driven transport controls
         (lock screen, Bluetooth, a paired watch) are shown through. Still zero
         network, zero storage, zero telemetry. -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <!-- Android 13+: without this, the foreground-service notification (and so the
         media transport controls it carries) is silently never shown. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- Required on Android 11+ (targetSdk 35): without this, package-visibility
         filtering hides the system TextToSpeech engine and the app is silently
         mute. Verbatim from the official TextToSpeech documentation. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.TTS_SERVICE" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="Lector"
        android:supportsRtl="true"
        android:theme="@style/Theme.Lector">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- Share sheet: "share to Lector" from any app -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
            <!-- Text-selection toolbar: select text anywhere → "Lector" -->
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

        <!-- Hosts the MediaSession; not started until playback begins. -->
        <service
            android:name=".playback.PlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
    </application>
</manifest>
